#!/usr/local/bin/perl

# A perl script to collect some information and place it in a html file

my $path_scriptout  =   "assets/html/scriptout.html"


sub gather_weather {
    my (country)    =   @_;
    #Use some webservice to grab the weather
}

sub read_file {
 my ($file)         =   @_;
    #The ready parts
}

sub write_file{
 my ($file, $data)  =   @_;
    #The writey parts
}
